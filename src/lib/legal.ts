import "server-only";

/**
 * ONE PLACE FOR THE FACTS BOTH LEGAL PAGES ASSERT.
 *
 * A privacy policy that describes practices the code does not have is the same
 * defect class as a screen that lies — with worse consequences, because a
 * regulator or a carrier reads this one. Every constant here was checked
 * against the schema and the send paths on 20 Aug 2026, and the page bodies
 * are written from them rather than from a template.
 *
 * `LEGAL_VERSION` is the date these were last reconciled against the code.
 * Change a data practice and change this, so the drift is visible.
 */
export const LEGAL_VERSION = "2026-08-20";

/** The only address the product publishes. There is no phone line. */
export const CONTACT_EMAIL = "hello@lakelife.ai";

/** Registered entity. Update if the legal name on the EIN letter differs. */
export const LEGAL_ENTITY = "LakeLife AI";
export const LEGAL_STATE = "Indiana";
